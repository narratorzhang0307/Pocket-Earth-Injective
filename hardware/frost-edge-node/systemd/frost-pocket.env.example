# Frost Edge Node sidecar configuration (copy to /etc/frost-pocket/frost-pocket.env, chmod 600).
# Never commit a real token.
FROST_FEED_URL=http://<pocket-earth-host>:<port>/api/frost-feed
FROST_FEED_TOKEN=<set-by-user>
